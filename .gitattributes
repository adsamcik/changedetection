# Auto detect text files and perform LF normalization
* text=auto

# Binary files - prevent line ending conversion and enable LFS-like handling
tests/ChangeDetection.Tests/Llm/Cache/llm-responses.db binary
