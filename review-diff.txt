diff --git a/src/ChangeDetection.Core/Entities/NotificationTemplate.cs b/src/ChangeDetection.Core/Entities/NotificationTemplate.cs
index 593f9c2..97e9221 100644
--- a/src/ChangeDetection.Core/Entities/NotificationTemplate.cs
+++ b/src/ChangeDetection.Core/Entities/NotificationTemplate.cs
@@ -104,7 +104,10 @@ public enum NotificationTemplateType
     ItemRemoved = 5,
 
     /// <summary>Schema drift detected (page structure changed).</summary>
-    SchemaDrift = 6
+    SchemaDrift = 6,
+
+    /// <summary>Job match alert from profile-based evaluation.</summary>
+    JobMatch = 7
 }
 
 /// <summary>
diff --git a/src/ChangeDetection.Core/Interfaces/IWatchGroupService.cs b/src/ChangeDetection.Core/Interfaces/IWatchGroupService.cs
index 7be5730..13cfcc9 100644
--- a/src/ChangeDetection.Core/Interfaces/IWatchGroupService.cs
+++ b/src/ChangeDetection.Core/Interfaces/IWatchGroupService.cs
@@ -22,5 +22,6 @@ public class WatchGroupCreateRequest
     public string? Description { get; set; }
     public string? Icon { get; set; }
     public string? UserIntent { get; set; }
+    public string? AnalysisProfileJson { get; set; }
     public List<string> Tags { get; set; } = [];
 }
diff --git a/src/ChangeDetection.Core/Interfaces/IWatchService.cs b/src/ChangeDetection.Core/Interfaces/IWatchService.cs
index dd2d1e0..9d27cb4 100644
--- a/src/ChangeDetection.Core/Interfaces/IWatchService.cs
+++ b/src/ChangeDetection.Core/Interfaces/IWatchService.cs
@@ -39,6 +39,16 @@ public class CreateWatchRequest
     public string? LlmProviderOverride { get; set; }
     public string? Description { get; set; }
     
+    /// <summary>
+    /// User's original monitoring intent for relevance scoring.
+    /// </summary>
+    public string? UserIntent { get; set; }
+
+    /// <summary>
+    /// Optional group membership for aggregate monitoring.
+    /// </summary>
+    public Guid? GroupId { get; set; }
+    
     /// <summary>
     /// Schedule settings for check frequency control.
     /// If null, defaults to fixed mode with the specified CheckInterval.
diff --git a/src/ChangeDetection/ChangeDetection.Client/Components/ProfileEditor.razor b/src/ChangeDetection/ChangeDetection.Client/Components/ProfileEditor.razor
new file mode 100644
index 0000000..c502f1e
--- /dev/null
+++ b/src/ChangeDetection/ChangeDetection.Client/Components/ProfileEditor.razor
@@ -0,0 +1,536 @@
+@using System.Text.Json
+@inject HttpClient Http
+
+<div class="profile-editor">
+    <div class="profile-header">
+        <h2>­ƒö¼ Candidate Profile</h2>
+        <p class="profile-subtitle">Define your qualifications for automated job matching</p>
+    </div>
+
+    @if (!string.IsNullOrEmpty(errorMessage))
+    {
+        <div class="alert alert-error" role="alert">@errorMessage</div>
+    }
+
+    <div class="profile-sections">
+        <!-- Education -->
+        <section class="form-section">
+            <h3>­ƒÄô Education</h3>
+            <div class="form-row">
+                <div class="form-group">
+                    <label for="edu-level">Degree Level</label>
+                    <select id="edu-level" class="form-control" @bind="model.EducationLevel" @bind:after="NotifyChanged">
+                        <option value="">Select...</option>
+                        <option value="BSc">BSc</option>
+                        <option value="MSc">MSc</option>
+                        <option value="PhD">PhD</option>
+                        <option value="MD">MD</option>
+                    </select>
+                    <p class="form-hint">Roles requiring a higher degree than yours will be auto-filtered</p>
+                </div>
+                <div class="form-group">
+                    <label for="edu-field">Field of Study</label>
+                    <input id="edu-field" type="text" class="form-control"
+                           placeholder="e.g. molecular and cell biology"
+                           @bind="model.EducationField" @bind:event="oninput" @bind:after="NotifyChanged" />
+                </div>
+            </div>
+        </section>
+
+        <!-- Experience -->
+        <section class="form-section">
+            <h3>­ƒÆ╝ Experience</h3>
+            <div class="form-row">
+                <div class="form-group">
+                    <label for="exp-years">Years of Experience</label>
+                    <input id="exp-years" type="text" class="form-control"
+                           placeholder="e.g. 1-3"
+                           @bind="model.ExperienceYears" @bind:event="oninput" @bind:after="NotifyChanged" />
+                </div>
+                <div class="form-group">
+                    <label for="current-role">Current Role (optional)</label>
+                    <input id="current-role" type="text" class="form-control"
+                           placeholder="e.g. Scientific Project Coordinator at ESGO"
+                           @bind="model.CurrentRole" @bind:event="oninput" @bind:after="NotifyChanged" />
+                </div>
+            </div>
+        </section>
+
+        <!-- Techniques -->
+        <section class="form-section">
+            <h3>­ƒº¼ Laboratory Techniques</h3>
+            <p class="section-description">Categorize your techniques by proficiency. Roles requiring skills in your "None" category will be automatically downgraded.</p>
+
+            <div class="technique-grid">
+                <div class="technique-column strong">
+                    <h4>Ô£à Strong <span class="count">(@model.TechniquesStrong.Count)</span></h4>
+                    <p class="column-hint">Hands-on, independent experience</p>
+                    <div class="tag-list">
+                        @foreach (var tech in model.TechniquesStrong)
+                        {
+                            <span class="tag tag-strong">
+                                @tech
+                                <button type="button" class="tag-remove" @onclick="() => RemoveTechnique(model.TechniquesStrong, tech)">├ù</button>
+                            </span>
+                        }
+                    </div>
+                    <div class="tag-input-wrapper">
+                        <input type="text" class="tag-input" placeholder="Add technique..."
+                               @bind="newTechStrong" @bind:event="oninput"
+                               @onkeydown="e => HandleTechKeyDown(e, model.TechniquesStrong, ref newTechStrong)" />
+                    </div>
+                </div>
+
+                <div class="technique-column basic">
+                    <h4>ÔÜí Basic <span class="count">(@model.TechniquesBasic.Count)</span></h4>
+                    <p class="column-hint">Some exposure, not independent</p>
+                    <div class="tag-list">
+                        @foreach (var tech in model.TechniquesBasic)
+                        {
+                            <span class="tag tag-basic">
+                                @tech
+                                <button type="button" class="tag-remove" @onclick="() => RemoveTechnique(model.TechniquesBasic, tech)">├ù</button>
+                            </span>
+                        }
+                    </div>
+                    <div class="tag-input-wrapper">
+                        <input type="text" class="tag-input" placeholder="Add technique..."
+                               @bind="newTechBasic" @bind:event="oninput"
+                               @onkeydown="e => HandleTechKeyDown(e, model.TechniquesBasic, ref newTechBasic)" />
+                    </div>
+                </div>
+
+                <div class="technique-column none">
+                    <h4>ÔØî None <span class="count">(@model.TechniquesNone.Count)</span></h4>
+                    <p class="column-hint">Not present ÔÇö auto-filter if required</p>
+                    <div class="tag-list">
+                        @foreach (var tech in model.TechniquesNone)
+                        {
+                            <span class="tag tag-none">
+                                @tech
+                                <button type="button" class="tag-remove" @onclick="() => RemoveTechnique(model.TechniquesNone, tech)">├ù</button>
+                            </span>
+                        }
+                    </div>
+                    <div class="tag-input-wrapper">
+                        <input type="text" class="tag-input" placeholder="Add technique..."
+                               @bind="newTechNone" @bind:event="oninput"
+                               @onkeydown="e => HandleTechKeyDown(e, model.TechniquesNone, ref newTechNone)" />
+                    </div>
+                </div>
+            </div>
+        </section>
+
+        <!-- Target Locations -->
+        <section class="form-section">
+            <h3>­ƒôì Target Locations</h3>
+            <p class="section-description">Roles outside these locations will be suppressed</p>
+            <div class="tag-list">
+                @foreach (var loc in model.TargetLocations)
+                {
+                    <span class="tag tag-location">
+                        @loc
+                        <button type="button" class="tag-remove" @onclick="() => RemoveLocation(loc)">├ù</button>
+                    </span>
+                }
+            </div>
+            <div class="tag-input-wrapper">
+                <input type="text" class="tag-input" placeholder="Add city (e.g. Prague, Copenhagen, Lyngby)..."
+                       @bind="newLocation" @bind:event="oninput"
+                       @onkeydown="HandleLocationKeyDown" />
+            </div>
+        </section>
+
+        <!-- Languages -->
+        <section class="form-section">
+            <h3>­ƒîÉ Languages</h3>
+            <div class="language-list">
+                @foreach (var lang in model.Languages)
+                {
+                    <div class="language-row">
+                        <input type="text" class="form-control lang-name" placeholder="Language"
+                               value="@lang.Key" disabled />
+                        <input type="text" class="form-control lang-level" placeholder="Level"
+                               value="@lang.Value" disabled />
+                        <button type="button" class="btn-icon" @onclick="() => RemoveLanguage(lang.Key)">├ù</button>
+                    </div>
+                }
+                <div class="language-row">
+                    <input type="text" class="form-control lang-name" placeholder="Language name"
+                           @bind="newLangName" @bind:event="oninput" />
+                    <input type="text" class="form-control lang-level" placeholder="Level (e.g. C1, native)"
+                           @bind="newLangLevel" @bind:event="oninput"
+                           @onkeydown="HandleLanguageKeyDown" />
+                    <button type="button" class="btn-icon btn-add" @onclick="AddLanguage">+</button>
+                </div>
+            </div>
+        </section>
+
+        <!-- Salary -->
+        <section class="form-section">
+            <h3>­ƒÆ░ Salary Floor</h3>
+            <p class="section-description">Minimum acceptable salary by region (roles below are flagged)</p>
+            <div class="form-row">
+                @foreach (var entry in model.SalaryFloor)
+                {
+                    <div class="form-group salary-entry">
+                        <label>@entry.Key</label>
+                        <input type="text" class="form-control" value="@entry.Value" disabled />
+                    </div>
+                }
+                <div class="form-group salary-entry">
+                    <label for="salary-region">Add region</label>
+                    <div class="salary-input-row">
+                        <input id="salary-region" type="text" class="form-control" placeholder="Region"
+                               @bind="newSalaryRegion" @bind:event="oninput" />
+                        <input type="text" class="form-control" placeholder="e.g. 50000 CZK"
+                               @bind="newSalaryValue" @bind:event="oninput"
+                               @onkeydown="HandleSalaryKeyDown" />
+                        <button type="button" class="btn-icon btn-add" @onclick="AddSalary">+</button>
+                    </div>
+                </div>
+            </div>
+        </section>
+
+        <!-- Dealbreakers -->
+        <section class="form-section">
+            <h3>­ƒÜ½ Dealbreakers</h3>
+            <p class="section-description">Companies or role types to automatically exclude</p>
+            <div class="tag-list">
+                @foreach (var db in model.Dealbreakers)
+                {
+                    <span class="tag tag-dealbreaker">
+                        @db
+                        <button type="button" class="tag-remove" @onclick="() => RemoveDealbreaker(db)">├ù</button>
+                    </span>
+                }
+            </div>
+            <div class="tag-input-wrapper">
+                <input type="text" class="tag-input" placeholder="Add dealbreaker (company name or role type)..."
+                       @bind="newDealbreaker" @bind:event="oninput"
+                       @onkeydown="HandleDealbreakerKeyDown" />
+            </div>
+        </section>
+
+        <!-- Preferences -->
+        <section class="form-section">
+            <h3>Ô¡É Preferences</h3>
+            <p class="section-description">Not dealbreakers, but strongly preferred (used for LLM scoring)</p>
+            <div class="tag-list">
+                @foreach (var pref in model.Preferences)
+                {
+                    <span class="tag tag-preference">
+                        @pref
+                        <button type="button" class="tag-remove" @onclick="() => RemovePreference(pref)">├ù</button>
+                    </span>
+                }
+            </div>
+            <div class="tag-input-wrapper">
+                <input type="text" class="tag-input" placeholder="Add preference..."
+                       @bind="newPreference" @bind:event="oninput"
+                       @onkeydown="HandlePreferenceKeyDown" />
+            </div>
+        </section>
+    </div>
+
+    <!-- Generated rules preview -->
+    @if (generatedRulesPreview.Count > 0)
+    {
+        <section class="form-section rules-preview">
+            <h3>­ƒñû Auto-Generated Filter Rules</h3>
+            <p class="section-description">These rules will be applied to every portal watch</p>
+            <div class="rules-list">
+                @foreach (var rule in generatedRulesPreview)
+                {
+                    <div class="rule-badge @rule.Type">
+                        <span class="rule-icon">@rule.Icon</span>
+                        <span class="rule-text">@rule.Text</span>
+                    </div>
+                }
+            </div>
+        </section>
+    }
+</div>
+
+@code {
+    [Parameter]
+    public string? ProfileJson { get; set; }
+
+    [Parameter]
+    public EventCallback<string> ProfileJsonChanged { get; set; }
+
+    [Parameter]
+    public EventCallback OnChanged { get; set; }
+
+    private ProfileModel model = new();
+    private string? errorMessage;
+
+    // Tag input state
+    private string newTechStrong = "";
+    private string newTechBasic = "";
+    private string newTechNone = "";
+    private string newLocation = "";
+    private string newDealbreaker = "";
+    private string newPreference = "";
+    private string newLangName = "";
+    private string newLangLevel = "";
+    private string newSalaryRegion = "";
+    private string newSalaryValue = "";
+
+    private List<RulePreview> generatedRulesPreview = [];
+
+    protected override void OnParametersSet()
+    {
+        if (!string.IsNullOrEmpty(ProfileJson))
+        {
+            try
+            {
+                model = ProfileModel.FromJson(ProfileJson);
+                UpdateRulesPreview();
+            }
+            catch { /* Keep default model on parse failure */ }
+        }
+    }
+
+    private async Task NotifyChanged()
+    {
+        var json = model.ToJson();
+        await ProfileJsonChanged.InvokeAsync(json);
+        await OnChanged.InvokeAsync();
+        UpdateRulesPreview();
+    }
+
+    private void UpdateRulesPreview()
+    {
+        generatedRulesPreview.Clear();
+
+        if (!string.IsNullOrEmpty(model.EducationLevel) && model.EducationLevel != "PhD")
+            generatedRulesPreview.Add(new("suppress", "­ƒÄô", $"PhD-required roles ÔåÆ auto-rejected (you have {model.EducationLevel})"));
+
+        foreach (var db in model.Dealbreakers)
+            generatedRulesPreview.Add(new("suppress", "­ƒÜ½", $"\"{db}\" ÔåÆ dealbreaker, auto-rejected"));
+
+        foreach (var tech in model.TechniquesNone)
+            generatedRulesPreview.Add(new("downgrade", "Ô¼ç´©Å", $"If requires \"{tech}\" ÔåÆ downgraded (skill gap)"));
+
+        if (model.TargetLocations.Count > 0)
+            generatedRulesPreview.Add(new("suppress", "­ƒôì", $"Locations not matching [{string.Join(", ", model.TargetLocations.Take(3))}{(model.TargetLocations.Count > 3 ? "..." : "")}] ÔåÆ suppressed"));
+    }
+
+    // Tag management helpers
+    private void HandleTechKeyDown(KeyboardEventArgs e, List<string> list, ref string input)
+    {
+        if (e.Key == "Enter" && !string.IsNullOrWhiteSpace(input))
+        {
+            AddTechnique(list, ref input);
+        }
+    }
+
+    private void AddTechnique(List<string> list, ref string input)
+    {
+        var value = input.Trim();
+        if (!string.IsNullOrEmpty(value) && !list.Contains(value, StringComparer.OrdinalIgnoreCase))
+        {
+            list.Add(value);
+            input = "";
+            _ = NotifyChanged();
+        }
+    }
+
+    private void RemoveTechnique(List<string> list, string tech)
+    {
+        list.Remove(tech);
+        _ = NotifyChanged();
+    }
+
+    private void HandleLocationKeyDown(KeyboardEventArgs e)
+    {
+        if (e.Key == "Enter" && !string.IsNullOrWhiteSpace(newLocation))
+        {
+            if (!model.TargetLocations.Contains(newLocation.Trim(), StringComparer.OrdinalIgnoreCase))
+            {
+                model.TargetLocations.Add(newLocation.Trim());
+                newLocation = "";
+                _ = NotifyChanged();
+            }
+        }
+    }
+
+    private void RemoveLocation(string loc)
+    {
+        model.TargetLocations.Remove(loc);
+        _ = NotifyChanged();
+    }
+
+    private void HandleDealbreakerKeyDown(KeyboardEventArgs e)
+    {
+        if (e.Key == "Enter" && !string.IsNullOrWhiteSpace(newDealbreaker))
+        {
+            if (!model.Dealbreakers.Contains(newDealbreaker.Trim(), StringComparer.OrdinalIgnoreCase))
+            {
+                model.Dealbreakers.Add(newDealbreaker.Trim());
+                newDealbreaker = "";
+                _ = NotifyChanged();
+            }
+        }
+    }
+
+    private void RemoveDealbreaker(string db)
+    {
+        model.Dealbreakers.Remove(db);
+        _ = NotifyChanged();
+    }
+
+    private void HandlePreferenceKeyDown(KeyboardEventArgs e)
+    {
+        if (e.Key == "Enter" && !string.IsNullOrWhiteSpace(newPreference))
+        {
+            if (!model.Preferences.Contains(newPreference.Trim(), StringComparer.OrdinalIgnoreCase))
+            {
+                model.Preferences.Add(newPreference.Trim());
+                newPreference = "";
+                _ = NotifyChanged();
+            }
+        }
+    }
+
+    private void RemovePreference(string pref)
+    {
+        model.Preferences.Remove(pref);
+        _ = NotifyChanged();
+    }
+
+    private void HandleLanguageKeyDown(KeyboardEventArgs e)
+    {
+        if (e.Key == "Enter") AddLanguage();
+    }
+
+    private void AddLanguage()
+    {
+        if (!string.IsNullOrWhiteSpace(newLangName) && !string.IsNullOrWhiteSpace(newLangLevel))
+        {
+            model.Languages[newLangName.Trim()] = newLangLevel.Trim();
+            newLangName = "";
+            newLangLevel = "";
+            _ = NotifyChanged();
+        }
+    }
+
+    private void RemoveLanguage(string key)
+    {
+        model.Languages.Remove(key);
+        _ = NotifyChanged();
+    }
+
+    private void HandleSalaryKeyDown(KeyboardEventArgs e)
+    {
+        if (e.Key == "Enter") AddSalary();
+    }
+
+    private void AddSalary()
+    {
+        if (!string.IsNullOrWhiteSpace(newSalaryRegion) && !string.IsNullOrWhiteSpace(newSalaryValue))
+        {
+            model.SalaryFloor[newSalaryRegion.Trim()] = newSalaryValue.Trim();
+            newSalaryRegion = "";
+            newSalaryValue = "";
+            _ = NotifyChanged();
+        }
+    }
+
+    private record RulePreview(string Type, string Icon, string Text);
+
+    /// <summary>
+    /// Client-side model that maps to/from the AnalysisProfileJson structure.
+    /// </summary>
+    public class ProfileModel
+    {
+        public string EducationLevel { get; set; } = "";
+        public string EducationField { get; set; } = "";
+        public string ExperienceYears { get; set; } = "";
+        public string CurrentRole { get; set; } = "";
+        public List<string> TechniquesStrong { get; set; } = [];
+        public List<string> TechniquesBasic { get; set; } = [];
+        public List<string> TechniquesNone { get; set; } = [];
+        public List<string> TargetLocations { get; set; } = [];
+        public Dictionary<string, string> Languages { get; set; } = [];
+        public Dictionary<string, string> SalaryFloor { get; set; } = [];
+        public List<string> Dealbreakers { get; set; } = [];
+        public List<string> Preferences { get; set; } = [];
+
+        public string ToJson()
+        {
+            var obj = new Dictionary<string, object>();
+
+            if (!string.IsNullOrEmpty(EducationLevel) || !string.IsNullOrEmpty(EducationField))
+            {
+                obj["education"] = new { level = EducationLevel, field = EducationField };
+            }
+            if (!string.IsNullOrEmpty(ExperienceYears))
+                obj["experience_years"] = ExperienceYears;
+            if (!string.IsNullOrEmpty(CurrentRole))
+                obj["current_role"] = CurrentRole;
+            if (TechniquesStrong.Count > 0)
+                obj["techniques_strong"] = TechniquesStrong;
+            if (TechniquesBasic.Count > 0)
+                obj["techniques_basic"] = TechniquesBasic;
+            if (TechniquesNone.Count > 0)
+                obj["techniques_none"] = TechniquesNone;
+            if (TargetLocations.Count > 0)
+                obj["target_locations"] = TargetLocations;
+            if (Languages.Count > 0)
+                obj["languages"] = Languages;
+            if (SalaryFloor.Count > 0)
+                obj["salary_floor"] = SalaryFloor;
+            if (Dealbreakers.Count > 0)
+                obj["dealbreakers"] = Dealbreakers;
+            if (Preferences.Count > 0)
+                obj["preferences"] = Preferences;
+
+            return JsonSerializer.Serialize(obj, new JsonSerializerOptions { WriteIndented = true });
+        }
+
+        public static ProfileModel FromJson(string json)
+        {
+            var model = new ProfileModel();
+            var doc = JsonDocument.Parse(json);
+            var root = doc.RootElement;
+
+            if (root.TryGetProperty("education", out var edu))
+            {
+                if (edu.TryGetProperty("level", out var lvl)) model.EducationLevel = lvl.GetString() ?? "";
+                if (edu.TryGetProperty("field", out var fld)) model.EducationField = fld.GetString() ?? "";
+            }
+            if (root.TryGetProperty("experience_years", out var exp))
+                model.ExperienceYears = exp.GetString() ?? "";
+            if (root.TryGetProperty("current_role", out var role))
+                model.CurrentRole = role.GetString() ?? "";
+
+            model.TechniquesStrong = GetStringList(root, "techniques_strong");
+            model.TechniquesBasic = GetStringList(root, "techniques_basic");
+            model.TechniquesNone = GetStringList(root, "techniques_none");
+            model.TargetLocations = GetStringList(root, "target_locations");
+            model.Dealbreakers = GetStringList(root, "dealbreakers");
+            model.Preferences = GetStringList(root, "preferences");
+
+            if (root.TryGetProperty("languages", out var langs) && langs.ValueKind == JsonValueKind.Object)
+                foreach (var prop in langs.EnumerateObject())
+                    model.Languages[prop.Name] = prop.Value.GetString() ?? "";
+
+            if (root.TryGetProperty("salary_floor", out var sal) && sal.ValueKind == JsonValueKind.Object)
+                foreach (var prop in sal.EnumerateObject())
+                    model.SalaryFloor[prop.Name] = prop.Value.ValueKind == JsonValueKind.Number
+                        ? prop.Value.GetInt64().ToString()
+                        : prop.Value.GetString() ?? "";
+
+            return model;
+        }
+
+        private static List<string> GetStringList(JsonElement root, string prop)
+        {
+            if (root.TryGetProperty(prop, out var arr) && arr.ValueKind == JsonValueKind.Array)
+                return arr.EnumerateArray().Select(e => e.GetString() ?? "").Where(s => s != "").ToList();
+            return [];
+        }
+    }
+}
diff --git a/src/ChangeDetection/ChangeDetection.Client/Components/ProfileEditor.razor.css b/src/ChangeDetection/ChangeDetection.Client/Components/ProfileEditor.razor.css
new file mode 100644
index 0000000..12145df
--- /dev/null
+++ b/src/ChangeDetection/ChangeDetection.Client/Components/ProfileEditor.razor.css
@@ -0,0 +1,332 @@
+.profile-editor {
+    display: flex;
+    flex-direction: column;
+    gap: var(--spacing-6, 1.5rem);
+}
+
+.profile-header h2 {
+    margin: 0;
+    font-size: 1.5rem;
+}
+
+.profile-subtitle {
+    margin: var(--spacing-1, 0.25rem) 0 0;
+    color: var(--color-text-muted, #475569);
+    font-size: var(--font-size-sm, 0.875rem);
+}
+
+.profile-sections {
+    display: flex;
+    flex-direction: column;
+    gap: var(--spacing-4, 1rem);
+}
+
+.form-section {
+    background: var(--color-bg, #fff);
+    border: 1px solid var(--color-border, #e2e8f0);
+    border-radius: var(--radius-lg, 12px);
+    padding: var(--spacing-6, 1.5rem);
+}
+
+.form-section h3 {
+    margin: 0 0 var(--spacing-2, 0.5rem);
+    font-size: 1.1rem;
+}
+
+.section-description {
+    margin: 0 0 var(--spacing-4, 1rem);
+    color: var(--color-text-muted, #475569);
+    font-size: var(--font-size-sm, 0.875rem);
+}
+
+.form-row {
+    display: grid;
+    grid-template-columns: 1fr 1fr;
+    gap: var(--spacing-4, 1rem);
+}
+
+@media (max-width: 640px) {
+    .form-row {
+        grid-template-columns: 1fr;
+    }
+}
+
+.form-group {
+    margin-bottom: var(--spacing-3, 0.75rem);
+}
+
+.form-group label {
+    display: block;
+    font-weight: 500;
+    margin-bottom: var(--spacing-1, 0.25rem);
+    font-size: var(--font-size-sm, 0.875rem);
+}
+
+.form-control {
+    width: 100%;
+    padding: 0.625rem 0.75rem;
+    font-size: var(--font-size-sm, 0.875rem);
+    border: 1px solid var(--color-border, #e2e8f0);
+    border-radius: var(--radius-sm, 6px);
+    background: var(--color-bg, #fff);
+    color: var(--color-text, #0f172a);
+    transition: border-color 0.15s ease;
+}
+
+.form-control:focus {
+    outline: none;
+    border-color: var(--color-primary, #0d9488);
+    box-shadow: 0 0 0 3px rgba(13, 148, 136, 0.1);
+}
+
+.form-hint {
+    margin: var(--spacing-1, 0.25rem) 0 0;
+    font-size: 0.8125rem;
+    color: var(--color-text-muted, #475569);
+}
+
+/* Technique Grid */
+.technique-grid {
+    display: grid;
+    grid-template-columns: 1fr 1fr 1fr;
+    gap: var(--spacing-4, 1rem);
+}
+
+@media (max-width: 768px) {
+    .technique-grid {
+        grid-template-columns: 1fr;
+    }
+}
+
+.technique-column {
+    border: 1px solid var(--color-border, #e2e8f0);
+    border-radius: var(--radius-md, 10px);
+    padding: var(--spacing-4, 1rem);
+    min-height: 160px;
+}
+
+.technique-column.strong {
+    border-color: var(--color-success, #10b981);
+    background: rgba(16, 185, 129, 0.03);
+}
+
+.technique-column.basic {
+    border-color: var(--color-warning, #f59e0b);
+    background: rgba(245, 158, 11, 0.03);
+}
+
+.technique-column.none {
+    border-color: var(--color-error, #ef4444);
+    background: rgba(239, 68, 68, 0.03);
+}
+
+.technique-column h4 {
+    margin: 0 0 var(--spacing-1, 0.25rem);
+    font-size: var(--font-size-sm, 0.875rem);
+}
+
+.technique-column .count {
+    color: var(--color-text-muted, #475569);
+    font-weight: 400;
+}
+
+.column-hint {
+    margin: 0 0 var(--spacing-3, 0.75rem);
+    font-size: 0.75rem;
+    color: var(--color-text-muted, #475569);
+}
+
+/* Tag Lists */
+.tag-list {
+    display: flex;
+    flex-wrap: wrap;
+    gap: var(--spacing-2, 0.5rem);
+    margin-bottom: var(--spacing-2, 0.5rem);
+}
+
+.tag {
+    display: inline-flex;
+    align-items: center;
+    gap: var(--spacing-1, 0.25rem);
+    padding: 0.25rem 0.625rem;
+    border-radius: 999px;
+    font-size: 0.8125rem;
+    font-weight: 500;
+    line-height: 1.4;
+}
+
+.tag-strong {
+    background: rgba(16, 185, 129, 0.12);
+    color: #065f46;
+}
+
+.tag-basic {
+    background: rgba(245, 158, 11, 0.12);
+    color: #92400e;
+}
+
+.tag-none {
+    background: rgba(239, 68, 68, 0.12);
+    color: #991b1b;
+}
+
+.tag-location {
+    background: rgba(6, 182, 212, 0.12);
+    color: #155e75;
+}
+
+.tag-dealbreaker {
+    background: rgba(239, 68, 68, 0.15);
+    color: #991b1b;
+    border: 1px solid rgba(239, 68, 68, 0.3);
+}
+
+.tag-preference {
+    background: rgba(245, 158, 11, 0.1);
+    color: #78350f;
+}
+
+.tag-remove {
+    background: none;
+    border: none;
+    cursor: pointer;
+    padding: 0 2px;
+    font-size: 1rem;
+    line-height: 1;
+    opacity: 0.5;
+    color: inherit;
+}
+
+.tag-remove:hover {
+    opacity: 1;
+}
+
+.tag-input-wrapper {
+    margin-top: var(--spacing-2, 0.5rem);
+}
+
+.tag-input {
+    width: 100%;
+    padding: 0.5rem 0.75rem;
+    border: 1px dashed var(--color-border, #e2e8f0);
+    border-radius: var(--radius-sm, 6px);
+    font-size: var(--font-size-sm, 0.875rem);
+    background: transparent;
+    color: var(--color-text, #0f172a);
+}
+
+.tag-input:focus {
+    outline: none;
+    border-color: var(--color-primary, #0d9488);
+    border-style: solid;
+}
+
+.tag-input::placeholder {
+    color: var(--color-text-muted, #475569);
+    opacity: 0.6;
+}
+
+/* Language & Salary Rows */
+.language-list {
+    display: flex;
+    flex-direction: column;
+    gap: var(--spacing-2, 0.5rem);
+}
+
+.language-row {
+    display: grid;
+    grid-template-columns: 1fr 1fr auto;
+    gap: var(--spacing-2, 0.5rem);
+    align-items: center;
+}
+
+.lang-name, .lang-level {
+    width: 100%;
+}
+
+.salary-entry {
+    flex: 1;
+}
+
+.salary-input-row {
+    display: flex;
+    gap: var(--spacing-2, 0.5rem);
+    align-items: center;
+}
+
+.salary-input-row .form-control {
+    flex: 1;
+}
+
+.btn-icon {
+    background: none;
+    border: 1px solid var(--color-border, #e2e8f0);
+    border-radius: var(--radius-sm, 6px);
+    width: 36px;
+    height: 36px;
+    cursor: pointer;
+    font-size: 1.1rem;
+    color: var(--color-text-muted, #475569);
+    display: flex;
+    align-items: center;
+    justify-content: center;
+}
+
+.btn-icon:hover {
+    background: var(--color-bg-secondary, #f8fafc);
+    border-color: var(--color-primary, #0d9488);
+    color: var(--color-primary, #0d9488);
+}
+
+.btn-add {
+    color: var(--color-primary, #0d9488);
+    border-color: var(--color-primary, #0d9488);
+}
+
+/* Rules Preview */
+.rules-preview {
+    background: var(--color-bg-secondary, #f8fafc);
+    border-style: dashed;
+}
+
+.rules-list {
+    display: flex;
+    flex-direction: column;
+    gap: var(--spacing-2, 0.5rem);
+}
+
+.rule-badge {
+    display: flex;
+    align-items: center;
+    gap: var(--spacing-2, 0.5rem);
+    padding: 0.5rem 0.75rem;
+    border-radius: var(--radius-sm, 6px);
+    font-size: var(--font-size-sm, 0.875rem);
+}
+
+.rule-badge.suppress {
+    background: rgba(239, 68, 68, 0.08);
+    color: #991b1b;
+}
+
+.rule-badge.downgrade {
+    background: rgba(245, 158, 11, 0.08);
+    color: #92400e;
+}
+
+.rule-icon {
+    flex-shrink: 0;
+}
+
+/* Alert */
+.alert {
+    padding: var(--spacing-3, 0.75rem) var(--spacing-4, 1rem);
+    border-radius: var(--radius-sm, 6px);
+    font-size: var(--font-size-sm, 0.875rem);
+}
+
+.alert-error {
+    background: rgba(239, 68, 68, 0.1);
+    color: #991b1b;
+    border: 1px solid rgba(239, 68, 68, 0.2);
+}
diff --git a/src/ChangeDetection/ChangeDetection.Client/Pages/JobWatchSetup.razor b/src/ChangeDetection/ChangeDetection.Client/Pages/JobWatchSetup.razor
new file mode 100644
index 0000000..19e8c45
--- /dev/null
+++ b/src/ChangeDetection/ChangeDetection.Client/Pages/JobWatchSetup.razor
@@ -0,0 +1,207 @@
+@page "/jobwatch"
+@page "/jobwatch/setup"
+@rendermode InteractiveAuto
+@inject HttpClient Http
+@inject NavigationManager Navigation
+
+<PageTitle>Job Watch Setup - Change Detection</PageTitle>
+
+<div class="jobwatch-setup">
+    <header class="page-header">
+        <div class="header-content">
+            <h1>­ƒö¼ Job Watch Setup</h1>
+            <p class="subtitle">Configure automated monitoring of biotech job portals with profile-based matching</p>
+        </div>
+    </header>
+
+    @if (!string.IsNullOrEmpty(errorMessage))
+    {
+        <div class="alert alert-error" role="alert">
+            <strong>Error:</strong> @errorMessage
+        </div>
+    }
+
+    @if (isSeeded)
+    {
+        <div class="success-banner">
+            <div class="success-icon">Ô£à</div>
+            <div class="success-content">
+                <h3>Job Watch Project Created!</h3>
+                <p>Monitoring @portalCount portals with your candidate profile. The system will check portals according to their configured schedule and alert you when matching jobs are found.</p>
+                <div class="success-actions">
+                    <a href="/" class="btn btn-primary">Go to Dashboard</a>
+                </div>
+            </div>
+        </div>
+    }
+    else
+    {
+        <div class="setup-layout">
+            <div class="setup-main">
+                <ProfileEditor @bind-ProfileJson="profileJson" OnChanged="OnProfileChanged" />
+            </div>
+
+            <aside class="setup-sidebar">
+                <div class="sidebar-section">
+                    <h3>Monitored Portals</h3>
+                    <p class="sidebar-hint">@portalCount portals will be monitored across Prague and Copenhagen</p>
+
+                    @if (portals.Count > 0)
+                    {
+                        <div class="portal-list">
+                            @foreach (var portal in portals)
+                            {
+                                <div class="portal-item @portal.Tier">
+                                    <div class="portal-info">
+                                        <span class="portal-name">@portal.Name</span>
+                                        <span class="portal-meta">@portal.CheckIntervalLabel ┬À @(portal.NeedsJavaScript ? "JS" : "HTML")</span>
+                                    </div>
+                                </div>
+                            }
+                        </div>
+                    }
+                    else
+                    {
+                        <button type="button" class="btn btn-outline btn-sm" @onclick="LoadPortals">
+                            Load portal list
+                        </button>
+                    }
+                </div>
+
+                <div class="sidebar-section">
+                    <h3>How It Works</h3>
+                    <div class="how-it-works">
+                        <div class="step">
+                            <span class="step-num">1</span>
+                            <span>Define your candidate profile</span>
+                        </div>
+                        <div class="step">
+                            <span class="step-num">2</span>
+                            <span>System creates watches for all portals</span>
+                        </div>
+                        <div class="step">
+                            <span class="step-num">3</span>
+                            <span>Each scan extracts new job listings</span>
+                        </div>
+                        <div class="step">
+                            <span class="step-num">4</span>
+                            <span>LLM validates matches against your profile</span>
+                        </div>
+                        <div class="step">
+                            <span class="step-num">5</span>
+                            <span>You get alerts only for qualified matches</span>
+                        </div>
+                    </div>
+                </div>
+
+                <div class="sidebar-section">
+                    <button type="button" class="btn btn-primary btn-block"
+                            disabled="@(isSubmitting || string.IsNullOrWhiteSpace(profileJson))"
+                            @onclick="CreateJobWatch">
+                        @if (isSubmitting)
+                        {
+                            <span class="spinner-sm"></span>
+                            <span>Creating project...</span>
+                        }
+                        else
+                        {
+                            <span>­ƒÜÇ Create Job Watch Project</span>
+                        }
+                    </button>
+                    @if (string.IsNullOrWhiteSpace(profileJson))
+                    {
+                        <p class="sidebar-hint">Fill in your profile to enable project creation</p>
+                    }
+                </div>
+            </aside>
+        </div>
+    }
+</div>
+
+@code {
+    private string? profileJson;
+    private string? errorMessage;
+    private bool isSubmitting;
+    private bool isSeeded;
+    private int portalCount = 17;
+    private List<PortalInfo> portals = [];
+
+    private async Task LoadPortals()
+    {
+        try
+        {
+            var response = await Http.GetAsync("/api/jobwatch/portals");
+            if (response.IsSuccessStatusCode)
+            {
+                portals = await response.Content.ReadFromJsonAsync<List<PortalInfo>>() ?? [];
+                portalCount = portals.Count;
+            }
+        }
+        catch (Exception ex)
+        {
+            errorMessage = ex.Message;
+        }
+    }
+
+    private void OnProfileChanged()
+    {
+        errorMessage = null;
+    }
+
+    private async Task CreateJobWatch()
+    {
+        if (string.IsNullOrWhiteSpace(profileJson))
+        {
+            errorMessage = "Please fill in your candidate profile before creating the project.";
+            return;
+        }
+
+        isSubmitting = true;
+        errorMessage = null;
+
+        try
+        {
+            var request = new
+            {
+                ProfileJson = profileJson,
+                UserIntent = "Monitor biotech/life-science job portals for matching positions based on candidate profile"
+            };
+
+            var response = await Http.PostAsJsonAsync("/api/jobwatch/seed", request);
+
+            if (response.IsSuccessStatusCode)
+            {
+                isSeeded = true;
+            }
+            else
+            {
+                errorMessage = await response.Content.ReadAsStringAsync();
+            }
+        }
+        catch (Exception ex)
+        {
+            errorMessage = $"Failed to create project: {ex.Message}";
+        }
+        finally
+        {
+            isSubmitting = false;
+        }
+    }
+
+    private class PortalInfo
+    {
+        public string Id { get; set; } = "";
+        public string Name { get; set; } = "";
+        public string Description { get; set; } = "";
+        public string Tier { get; set; } = "";
+        public double CheckIntervalHours { get; set; }
+        public bool NeedsJavaScript { get; set; }
+
+        public string CheckIntervalLabel => CheckIntervalHours switch
+        {
+            <= 24 => "Daily",
+            <= 48 => "Every 2 days",
+            _ => "Weekly"
+        };
+    }
+}
diff --git a/src/ChangeDetection/ChangeDetection.Client/Pages/JobWatchSetup.razor.css b/src/ChangeDetection/ChangeDetection.Client/Pages/JobWatchSetup.razor.css
new file mode 100644
index 0000000..4e665cc
--- /dev/null
+++ b/src/ChangeDetection/ChangeDetection.Client/Pages/JobWatchSetup.razor.css
@@ -0,0 +1,234 @@
+.jobwatch-setup {
+    max-width: 1200px;
+    margin: 0 auto;
+    padding: var(--spacing-6, 1.5rem);
+}
+
+.page-header {
+    margin-bottom: var(--spacing-6, 1.5rem);
+}
+
+.page-header h1 {
+    margin: 0;
+    font-size: 1.75rem;
+}
+
+.subtitle {
+    margin: var(--spacing-1, 0.25rem) 0 0;
+    color: var(--color-text-muted, #475569);
+}
+
+/* Layout */
+.setup-layout {
+    display: grid;
+    grid-template-columns: 1fr 320px;
+    gap: var(--spacing-6, 1.5rem);
+    align-items: start;
+}
+
+@media (max-width: 960px) {
+    .setup-layout {
+        grid-template-columns: 1fr;
+    }
+}
+
+.setup-main {
+    min-width: 0;
+}
+
+/* Sidebar */
+.setup-sidebar {
+    display: flex;
+    flex-direction: column;
+    gap: var(--spacing-4, 1rem);
+    position: sticky;
+    top: var(--spacing-6, 1.5rem);
+}
+
+.sidebar-section {
+    background: var(--color-bg, #fff);
+    border: 1px solid var(--color-border, #e2e8f0);
+    border-radius: var(--radius-lg, 12px);
+    padding: var(--spacing-4, 1rem);
+}
+
+.sidebar-section h3 {
+    margin: 0 0 var(--spacing-2, 0.5rem);
+    font-size: 1rem;
+}
+
+.sidebar-hint {
+    font-size: 0.8125rem;
+    color: var(--color-text-muted, #475569);
+    margin: var(--spacing-2, 0.5rem) 0 0;
+}
+
+/* Portal list */
+.portal-list {
+    display: flex;
+    flex-direction: column;
+    gap: var(--spacing-1, 0.25rem);
+    max-height: 300px;
+    overflow-y: auto;
+    margin-top: var(--spacing-2, 0.5rem);
+}
+
+.portal-item {
+    padding: var(--spacing-2, 0.5rem);
+    border-radius: var(--radius-sm, 6px);
+    border-left: 3px solid transparent;
+}
+
+.portal-item.tier-1 {
+    border-left-color: var(--color-primary, #0d9488);
+}
+
+.portal-item.tier-2 {
+    border-left-color: var(--color-text-muted, #475569);
+}
+
+.portal-name {
+    display: block;
+    font-size: var(--font-size-sm, 0.875rem);
+    font-weight: 500;
+}
+
+.portal-meta {
+    font-size: 0.75rem;
+    color: var(--color-text-muted, #475569);
+}
+
+/* How it works */
+.how-it-works {
+    display: flex;
+    flex-direction: column;
+    gap: var(--spacing-3, 0.75rem);
+    margin-top: var(--spacing-2, 0.5rem);
+}
+
+.step {
+    display: flex;
+    align-items: center;
+    gap: var(--spacing-3, 0.75rem);
+    font-size: var(--font-size-sm, 0.875rem);
+}
+
+.step-num {
+    width: 24px;
+    height: 24px;
+    border-radius: 50%;
+    background: var(--color-primary, #0d9488);
+    color: white;
+    display: flex;
+    align-items: center;
+    justify-content: center;
+    font-size: 0.75rem;
+    font-weight: 600;
+    flex-shrink: 0;
+}
+
+/* Buttons */
+.btn {
+    padding: 0.75rem 1.5rem;
+    border: none;
+    border-radius: var(--radius-md, 10px);
+    cursor: pointer;
+    font-weight: 500;
+    font-size: var(--font-size-sm, 0.875rem);
+    transition: all 0.15s ease;
+    display: inline-flex;
+    align-items: center;
+    justify-content: center;
+    gap: var(--spacing-2, 0.5rem);
+    text-decoration: none;
+}
+
+.btn-primary {
+    background: var(--color-primary, #0d9488);
+    color: white;
+}
+
+.btn-primary:hover:not(:disabled) {
+    background: var(--color-primary-hover, #0f766e);
+}
+
+.btn-primary:disabled {
+    opacity: 0.5;
+    cursor: not-allowed;
+}
+
+.btn-outline {
+    background: transparent;
+    border: 1px solid var(--color-border, #e2e8f0);
+    color: var(--color-text, #0f172a);
+}
+
+.btn-outline:hover {
+    background: var(--color-bg-secondary, #f8fafc);
+}
+
+.btn-block {
+    width: 100%;
+}
+
+.btn-sm {
+    padding: 0.5rem 1rem;
+    font-size: 0.8125rem;
+}
+
+.spinner-sm {
+    width: 16px;
+    height: 16px;
+    border: 2px solid rgba(255, 255, 255, 0.3);
+    border-top-color: white;
+    border-radius: 50%;
+    animation: spin 0.6s linear infinite;
+}
+
+@keyframes spin {
+    to { transform: rotate(360deg); }
+}
+
+/* Success */
+.success-banner {
+    display: flex;
+    gap: var(--spacing-4, 1rem);
+    background: rgba(16, 185, 129, 0.08);
+    border: 1px solid rgba(16, 185, 129, 0.2);
+    border-radius: var(--radius-lg, 12px);
+    padding: var(--spacing-6, 1.5rem);
+}
+
+.success-icon {
+    font-size: 2rem;
+    flex-shrink: 0;
+}
+
+.success-content h3 {
+    margin: 0 0 var(--spacing-2, 0.5rem);
+    color: #065f46;
+}
+
+.success-content p {
+    margin: 0 0 var(--spacing-4, 1rem);
+    color: var(--color-text, #0f172a);
+}
+
+.success-actions {
+    display: flex;
+    gap: var(--spacing-2, 0.5rem);
+}
+
+/* Alerts */
+.alert {
+    padding: var(--spacing-3, 0.75rem) var(--spacing-4, 1rem);
+    border-radius: var(--radius-sm, 6px);
+    font-size: var(--font-size-sm, 0.875rem);
+    margin-bottom: var(--spacing-4, 1rem);
+}
+
+.alert-error {
+    background: rgba(239, 68, 68, 0.1);
+    color: #991b1b;
+    border: 1px solid rgba(239, 68, 68, 0.2);
+}
diff --git a/src/ChangeDetection/ChangeDetection/Endpoints/JobWatchEndpoints.cs b/src/ChangeDetection/ChangeDetection/Endpoints/JobWatchEndpoints.cs
new file mode 100644
index 0000000..ad3a4bf
--- /dev/null
+++ b/src/ChangeDetection/ChangeDetection/Endpoints/JobWatchEndpoints.cs
@@ -0,0 +1,67 @@
+using ChangeDetection.Services.JobWatch;
+
+namespace ChangeDetection.Endpoints;
+
+/// <summary>
+/// API endpoints for the Job Watch feature ÔÇö creates and manages a job search project
+/// with portal watches, candidate profiles, and profile-based filtering.
+/// </summary>
+public static class JobWatchEndpoints
+{
+    public static void MapJobWatchEndpoints(this IEndpointRouteBuilder routes)
+    {
+        var group = routes.MapGroup("/api/jobwatch")
+            .WithTags("JobWatch");
+
+        group.MapPost("/seed", SeedJobWatch)
+            .WithName("SeedJobWatch")
+            .WithDescription("Create a complete job watch project with all portal configurations");
+
+        group.MapGet("/portals", GetPortalDefinitions)
+            .WithName("GetPortalDefinitions")
+            .WithDescription("List all configured portal definitions and their schemas");
+    }
+
+    private static async Task<IResult> SeedJobWatch(
+        JobWatchSeedRequest request,
+        JobWatchSeeder seeder,
+        CancellationToken ct)
+    {
+        if (string.IsNullOrWhiteSpace(request.ProfileJson))
+            return Results.BadRequest("ProfileJson is required");
+
+        var group = await seeder.SeedAsync(
+            request.ProfileJson,
+            request.UserIntent ?? "Monitor biotech/life-science job portals for matching positions",
+            ct);
+
+        return Results.Created($"/api/groups/{group.Id}", new
+        {
+            GroupId = group.Id.ToString(),
+            group.Name,
+            PortalCount = JobWatchSeeder.GetAllPortalDefinitions().Count
+        });
+    }
+
+    private static IResult GetPortalDefinitions()
+    {
+        var portals = JobWatchSeeder.GetAllPortalDefinitions();
+        return Results.Ok(portals.Select(p => new
+        {
+            p.Id,
+            p.Name,
+            p.Description,
+            p.Url,
+            p.Tier,
+            CheckIntervalHours = p.CheckInterval.TotalHours,
+            NeedsJavaScript = p.FetchSettings.UseJavaScript,
+            SchemaFields = p.Schema.Fields.Select(f => new { f.Name, Type = f.Type.ToString() })
+        }));
+    }
+}
+
+public class JobWatchSeedRequest
+{
+    public required string ProfileJson { get; set; }
+    public string? UserIntent { get; set; }
+}
diff --git a/src/ChangeDetection/ChangeDetection/Program.cs b/src/ChangeDetection/ChangeDetection/Program.cs
index 24f4b4c..b72bdef 100644
--- a/src/ChangeDetection/ChangeDetection/Program.cs
+++ b/src/ChangeDetection/ChangeDetection/Program.cs
@@ -17,6 +17,7 @@ using ChangeDetection.Services.Persistence;
 using ChangeDetection.Services.Pipeline;
 using ChangeDetection.Services.Scraping;
 using ChangeDetection.Services.SetupPipeline;
+using ChangeDetection.Services.JobWatch;
 using ChangeDetection.Core.Pipeline;
 using ChangeDetection.Core.Pipeline.Setup;
 using ChangeDetection.Core.Pipeline.Validation;
@@ -373,6 +374,7 @@ builder.Services.AddScoped<IObjectDiffService, ObjectDiffService>();
 builder.Services.AddScoped<IFilterEvaluationService, FilterEvaluationService>();
 builder.Services.AddScoped<IErrorResolutionService, ErrorResolutionService>();
 builder.Services.AddSingleton<IProfileFilterRuleGenerator, ProfileFilterRuleGenerator>();
+builder.Services.AddScoped<JobWatchSeeder>();
 
 // Auto-healing services
 builder.Services.AddScoped<IAutoHealingService, AutoHealingService>();
@@ -503,6 +505,9 @@ app.MapGroup("/api/health")
     .RequireAdminInSsoMode(builder.Configuration)
     .MapDatabaseHealthEndpoints();
 
+// Job Watch feature
+app.MapJobWatchEndpoints();
+
 // Map SignalR hub
 app.MapHub<ChangeDetectionHub>("/hubs/changes")
     .RequireAuthenticationInSsoMode(builder.Configuration);
diff --git a/src/ChangeDetection/ChangeDetection/Services/JobWatch/JobWatchSeeder.cs b/src/ChangeDetection/ChangeDetection/Services/JobWatch/JobWatchSeeder.cs
new file mode 100644
index 0000000..2e1efc3
--- /dev/null
+++ b/src/ChangeDetection/ChangeDetection/Services/JobWatch/JobWatchSeeder.cs
@@ -0,0 +1,494 @@
+using System.Text.Json;
+using ChangeDetection.Core.Entities;
+using ChangeDetection.Core.Interfaces;
+
+namespace ChangeDetection.Services.JobWatch;
+
+/// <summary>
+/// Seeds the biotech job search watch group with all portal configurations.
+/// Creates a WatchGroup with candidate profile, then individual watches per portal
+/// with appropriate ExtractionSchemas, FetchSettings, and FilterRules.
+/// </summary>
+public class JobWatchSeeder(
+    IWatchGroupService groupService,
+    IWatchService watchService,
+    IProfileFilterRuleGenerator filterRuleGenerator,
+    ILogger<JobWatchSeeder> logger)
+{
+    /// <summary>
+    /// Create the full job watch project: group + profile + watches + filter rules.
+    /// </summary>
+    public async Task<WatchGroup> SeedAsync(
+        string profileJson,
+        string userIntent,
+        CancellationToken ct = default)
+    {
+        logger.LogInformation("Seeding biotech job search project");
+
+        var group = await groupService.CreateGroupAsync(new WatchGroupCreateRequest
+        {
+            Name = "Biotech Job Search ÔÇö Prague + Copenhagen",
+            Description = "Automated monitoring of biotech/life-science job portals across Prague and the Copenhagen/Medicon Valley area",
+            Icon = "­ƒö¼",
+            UserIntent = userIntent,
+            AnalysisProfileJson = profileJson,
+            Tags = ["job-search", "biotech", "life-science", "prague", "copenhagen"]
+        }, ct);
+
+        var profileRules = filterRuleGenerator.GenerateRules(profileJson);
+
+        var portals = GetAllPortalDefinitions();
+        foreach (var portal in portals)
+        {
+            try
+            {
+                await CreatePortalWatchAsync(group.Id, portal, userIntent, profileRules, ct);
+                logger.LogInformation("Created portal watch: {Name} ({Url})", portal.Name, portal.Url);
+            }
+            catch (Exception ex)
+            {
+                logger.LogWarning(ex, "Failed to create portal watch: {Name}", portal.Name);
+            }
+        }
+
+        logger.LogInformation("Job search project seeded with {Count} portal watches", portals.Count);
+        return group;
+    }
+
+    private async Task CreatePortalWatchAsync(
+        Guid groupId,
+        PortalDefinition portal,
+        string userIntent,
+        List<FilterRule> profileRules,
+        CancellationToken ct)
+    {
+        await watchService.CreateWatchAsync(new CreateWatchRequest
+        {
+            Url = portal.Url,
+            Name = portal.Name,
+            Description = portal.Description,
+            UserIntent = userIntent,
+            GroupId = groupId,
+            Tags = ["job-search", "biotech", portal.Tier, ..portal.ExtraTags],
+            CheckInterval = portal.CheckInterval,
+            ScheduleSettings = new CheckScheduleSettings
+            {
+                Mode = CheckScheduleMode.Fixed,
+                BaseInterval = portal.CheckInterval
+            },
+            FetchSettings = portal.FetchSettings,
+            SchemaEnabled = true,
+            Schema = portal.Schema,
+            FilterRules = profileRules.Select(CloneRule).ToList()
+        }, ct);
+    }
+
+    /// <summary>
+    /// Returns all portal definitions with their extraction schemas.
+    /// </summary>
+    public static List<PortalDefinition> GetAllPortalDefinitions()
+    {
+        var daily = TimeSpan.FromHours(24);
+        var twoDays = TimeSpan.FromHours(48);
+        var weekly = TimeSpan.FromDays(7);
+
+        var standardFetch = new FetchSettings { TimeoutSeconds = 30 };
+        var jsFetch = new FetchSettings
+        {
+            UseJavaScript = true,
+            TimeoutSeconds = 45,
+            WaitAfterLoadMs = 3000
+        };
+        var heavyJsFetch = new FetchSettings
+        {
+            UseJavaScript = true,
+            TimeoutSeconds = 60,
+            WaitAfterLoadMs = 5000
+        };
+
+        return
+        [
+            // ===== TIER 1: Primary portals (daily / every-2-days) =====
+
+            new()
+            {
+                Id = "watch-ucph",
+                Name = "University of Copenhagen Vacancies",
+                Description = "Lab/research roles in Faculty of Health, BRIC, NNF Center at UCPH",
+                Url = "https://employment.ku.dk/all-vacancies/",
+                Tier = "tier-1",
+                CheckInterval = daily,
+                FetchSettings = standardFetch,
+                Schema = BuildJobSchema(
+                    "table.table tbody tr, .vacancy-list .vacancy-item, article.vacancy",
+                    "td:first-child a, .vacancy-title a, h2 a",
+                    company: "td:nth-child(2), .department, .faculty",
+                    location: "td:nth-child(3), .location",
+                    url: "td:first-child a, .vacancy-title a",
+                    posted: "td:last-child, .date, time",
+                    deadline: ".deadline, .application-deadline"),
+                ExtraTags = ["denmark", "academia"]
+            },
+            new()
+            {
+                Id = "watch-novo",
+                Name = "Novo Nordisk Denmark",
+                Description = "Scientist/lab tech roles at Novo Nordisk Denmark sites",
+                Url = "https://careers.novonordisk.com/global/en/search-results?keywords=laboratory%20scientist&location=Denmark",
+                Tier = "tier-1",
+                CheckInterval = daily,
+                FetchSettings = heavyJsFetch,
+                Schema = BuildJobSchema(
+                    ".job-card, .search-result-item, [data-ph-at-id='job-card']",
+                    ".job-title a, .job-card-title, [data-ph-at-id='job-title']",
+                    location: ".job-location, [data-ph-at-id='job-location']",
+                    url: ".job-title a, [data-ph-at-id='job-title'] a",
+                    posted: ".job-date, .posted-date"),
+                ExtraTags = ["denmark", "pharma", "novo-nordisk"]
+            },
+            new()
+            {
+                Id = "watch-novonesis",
+                Name = "Novonesis Careers Denmark",
+                Description = "R&D/lab roles at Novonesis Denmark",
+                Url = "https://www.novonesis.com/en/careers/jobs?location=Denmark",
+                Tier = "tier-1",
+                CheckInterval = daily,
+                FetchSettings = jsFetch,
+                Schema = BuildJobSchema(
+                    ".job-card, .job-list-item, article.job",
+                    ".job-title, h3, h2 a",
+                    location: ".job-location, .location",
+                    url: "a.job-title, a.job-link, h3 a"),
+                ExtraTags = ["denmark", "biotech"]
+            },
+            new()
+            {
+                Id = "watch-szu",
+                Name = "SZ├Ü Kari├®ra (Czech State Health Institute)",
+                Description = "Diagnostics/lab roles at Czech State Health Institute",
+                Url = "https://szu.gov.cz/statni-zdravotni-ustav-starame-se-o-zdrave-cesko/kariera/",
+                Tier = "tier-1",
+                CheckInterval = twoDays,
+                FetchSettings = standardFetch,
+                Schema = BuildJobSchema(
+                    "article, .career-item, .job-listing, li.vacancy, .wp-block-list li",
+                    "h2 a, h3 a, a.career-link, a",
+                    location: ".location, .mesto",
+                    url: "h2 a, h3 a, a",
+                    posted: ".date, time, .datum",
+                    deadline: ".deadline, .uzaverka"),
+                ExtraTags = ["czech", "public-sector", "diagnostics"]
+            },
+            new()
+            {
+                Id = "watch-img",
+                Name = "IMG / CCP Careers (Academy of Sciences)",
+                Description = "Positions at Academy of Sciences / Czech Centre for Phenogenomics",
+                Url = "https://www.img.cas.cz/kariera/volna-mista/",
+                Tier = "tier-1",
+                CheckInterval = daily,
+                FetchSettings = standardFetch,
+                Schema = BuildJobSchema(
+                    "article, .position-item, li.vacancy, .career-list li",
+                    "h2 a, h3 a, a, .position-title",
+                    company: ".department, .oddeleni",
+                    url: "h2 a, h3 a, a",
+                    posted: ".date, time"),
+                ExtraTags = ["czech", "academia", "phenogenomics"]
+            },
+            new()
+            {
+                Id = "watch-jobscz",
+                Name = "Jobs.cz Lab/Chemistry Prague",
+                Description = "Lab/chemist/analyst roles in Prague from Jobs.cz",
+                Url = "https://www.jobs.cz/prace/?profession%5B0%5D=201100032&profession%5B1%5D=201100139&locality%5Bcode%5D=3468",
+                Tier = "tier-1",
+                CheckInterval = daily,
+                FetchSettings = jsFetch,
+                Schema = BuildJobSchema(
+                    ".search-list__item, article.job, .standalone-search-item",
+                    "h2 a, .search-list__main-info__title a, .job-title a",
+                    company: ".search-list__main-info__company, .company-name",
+                    location: ".search-list__main-info__address, .location",
+                    url: "h2 a, .search-list__main-info__title a",
+                    posted: ".search-list__advert-age, .date"),
+                ExtraTags = ["czech", "prague", "job-board"]
+            },
+            new()
+            {
+                Id = "watch-lund",
+                Name = "Lund University Vacancies",
+                Description = "Research/lab positions at Lund University (Medicon Valley)",
+                Url = "https://www.lunduniversity.lu.se/vacancies",
+                Tier = "tier-1",
+                CheckInterval = daily,
+                FetchSettings = jsFetch,
+                Schema = BuildJobSchema(
+                    "table tbody tr, .vacancy-row, .job-list-item",
+                    "td a, .title a, h3 a",
+                    company: ".department, .faculty, td:nth-child(2)",
+                    location: "td:nth-child(3), .location",
+                    url: "td a, .title a",
+                    posted: "td:nth-child(4), .date",
+                    deadline: "td:last-child, .deadline"),
+                ExtraTags = ["sweden", "academia", "medicon-valley"]
+            },
+            new()
+            {
+                Id = "watch-gcr",
+                Name = "Greater Copenhagen Region Careers",
+                Description = "Cross-border DK+SE life-science roles for internationals",
+                Url = "https://careerportal.greatercphregion.com/",
+                Tier = "tier-1",
+                CheckInterval = daily,
+                FetchSettings = jsFetch,
+                Schema = BuildJobSchema(
+                    ".job-card, article.job, .listing-item",
+                    "h2, h3, .job-title, a.title",
+                    company: ".company, .employer",
+                    location: ".location, .city",
+                    url: "a.job-link, h2 a, h3 a"),
+                ExtraTags = ["denmark", "sweden", "cross-border"]
+            },
+            new()
+            {
+                Id = "watch-medicon-village",
+                Name = "Medicon Village Open Positions",
+                Description = "Lab/science positions in Medicon Village cluster (Lund, Sweden)",
+                Url = "https://www.mediconvillage.se/open-positions/",
+                Tier = "tier-1",
+                CheckInterval = daily,
+                FetchSettings = standardFetch,
+                Schema = BuildJobSchema(
+                    "article, .position-item, .job-listing, li.job",
+                    "h2 a, h3 a, .title a",
+                    company: ".company, .organization",
+                    url: "h2 a, h3 a, a"),
+                ExtraTags = ["sweden", "medicon-valley", "biotech-cluster"]
+            },
+            new()
+            {
+                Id = "watch-lundbeck",
+                Name = "Lundbeck Careers Copenhagen",
+                Description = "Scientist/lab roles at Lundbeck Copenhagen",
+                Url = "https://jobs.lundbeck.com/search/?q=scientist+laboratory&locationsearch=copenhagen",
+                Tier = "tier-1",
+                CheckInterval = daily,
+                FetchSettings = jsFetch,
+                Schema = BuildJobSchema(
+                    ".job-card, .search-result, tr.job-row, .job-listing",
+                    ".job-title a, td.title a, h3 a",
+                    location: ".job-location, td.location, .location",
+                    url: ".job-title a, td.title a, h3 a",
+                    posted: ".date, td.date"),
+                ExtraTags = ["denmark", "pharma", "neuroscience"]
+            },
+
+            // ===== TIER 2: Supplementary portals (weekly) =====
+
+            new()
+            {
+                Id = "watch-bavarian",
+                Name = "Bavarian Nordic Careers (Workday)",
+                Description = "Lab/QC roles at vaccine company near Copenhagen",
+                Url = "https://bavariannordic.wd103.myworkdayjobs.com/en-US/BavarianNordic",
+                Tier = "tier-2",
+                CheckInterval = weekly,
+                FetchSettings = heavyJsFetch,
+                Schema = BuildJobSchema(
+                    "[data-automation-id='jobItem'], .job-card, li.css-1q2dra3",
+                    "a[data-automation-id='jobTitle'], .job-title a",
+                    location: "[data-automation-id='locations'], .location",
+                    url: "a[data-automation-id='jobTitle']",
+                    posted: "[data-automation-id='postedOn'], .date"),
+                ExtraTags = ["denmark", "vaccines", "workday"]
+            },
+            new()
+            {
+                Id = "watch-genmab",
+                Name = "Genmab Careers Copenhagen",
+                Description = "Lab roles at antibody biotech (watch for non-senior roles)",
+                Url = "https://careers.genmab.com/search/?searchby=location&d=10&lat=55.6761&lon=12.5683",
+                Tier = "tier-2",
+                CheckInterval = weekly,
+                FetchSettings = jsFetch,
+                Schema = BuildJobSchema(
+                    ".job-card, .search-result-item, tr.job-row",
+                    ".job-title a, td a, h3 a",
+                    location: ".job-location, .location",
+                    url: ".job-title a, td a, h3 a"),
+                ExtraTags = ["denmark", "antibody", "biotech"]
+            },
+            new()
+            {
+                Id = "watch-ferring",
+                Name = "Ferring Pharmaceuticals Careers",
+                Description = "Lab roles at Ferring Copenhagen",
+                Url = "https://careers.ferring.com/search/?q=scientist&locationsearch=copenhagen",
+                Tier = "tier-2",
+                CheckInterval = weekly,
+                FetchSettings = jsFetch,
+                Schema = BuildJobSchema(
+                    ".job-card, .search-result, tr.job-row",
+                    ".job-title a, td a, h3 a",
+                    location: ".job-location, .location",
+                    url: ".job-title a, td a, h3 a"),
+                ExtraTags = ["denmark", "pharma"]
+            },
+            new()
+            {
+                Id = "watch-alk",
+                Name = "ALK-Abell├│ Careers (H├©rsholm)",
+                Description = "Lab/immunology roles at ALK",
+                Url = "https://alkabello.easycruit.com/index.html",
+                Tier = "tier-2",
+                CheckInterval = weekly,
+                FetchSettings = jsFetch,
+                Schema = BuildJobSchema(
+                    ".vacancy-item, .job-listing, tr.vacancy, li.position",
+                    "a.vacancy-title, .title a, td a",
+                    location: ".location, .region",
+                    url: "a.vacancy-title, .title a",
+                    deadline: ".deadline"),
+                ExtraTags = ["denmark", "allergy", "immunotherapy"]
+            },
+            new()
+            {
+                Id = "watch-ssi",
+                Name = "Statens Serum Institut Jobs",
+                Description = "Virology/diagnostics lab roles at Danish SSI",
+                Url = "https://www.ssi.dk/om-ssi/job-i-ssi",
+                Tier = "tier-2",
+                CheckInterval = weekly,
+                FetchSettings = standardFetch,
+                Schema = BuildJobSchema(
+                    "article, .job-item, li.vacancy, .content-list li",
+                    "h2 a, h3 a, a.job-link",
+                    company: ".department",
+                    url: "h2 a, h3 a, a",
+                    posted: ".date, time",
+                    deadline: ".deadline, .ansogningsfrist"),
+                ExtraTags = ["denmark", "public-sector", "virology"]
+            },
+            new()
+            {
+                Id = "watch-eures",
+                Name = "EURES Life Science Jobs",
+                Description = "EU cross-border life-science positions via EURES",
+                Url = "https://eures.europa.eu/eures-services/eures-targeted-mobility-scheme_en",
+                Tier = "tier-2",
+                CheckInterval = weekly,
+                FetchSettings = jsFetch,
+                Schema = BuildJobSchema(
+                    ".job-card, article.job, .listing-item, .search-result",
+                    "h2 a, h3 a, .job-title",
+                    company: ".company, .employer",
+                    location: ".location, .country",
+                    url: "a.job-link, h2 a"),
+                ExtraTags = ["eu", "cross-border", "mobility"]
+            },
+            new()
+            {
+                Id = "watch-jobindex",
+                Name = "Jobindex Life Science Denmark",
+                Description = "Broad Danish lab/science job board",
+                Url = "https://www.jobindex.dk/jobsoegning?q=laboratory+scientist&area=2",
+                Tier = "tier-2",
+                CheckInterval = weekly,
+                FetchSettings = jsFetch,
+                Schema = BuildJobSchema(
+                    ".jobsearch-result, .PaidJob, article.job, div.jix_robotjob",
+                    "h4 a, .jix_robotjob--link, .job-title a",
+                    company: ".jix_robotjob--company, .company a",
+                    location: ".jix_robotjob--area, .location",
+                    url: "h4 a, .jix_robotjob--link a",
+                    posted: ".jix_robotjob--published-date, .date",
+                    deadline: ".deadline, .ansogningsfrist"),
+                ExtraTags = ["denmark", "job-board"]
+            }
+        ];
+    }
+
+    /// <summary>
+    /// Build a job listing ExtractionSchema with fallback CSS selectors.
+    /// </summary>
+    private static ExtractionSchema BuildJobSchema(
+        string itemSelector,
+        string titleSelector,
+        string? company = null,
+        string? location = null,
+        string? url = null,
+        string? posted = null,
+        string? deadline = null)
+    {
+        var fields = new List<SchemaField>
+        {
+            new() { Name = "title", Type = FieldType.String, Selector = titleSelector, IsRequired = true, IsIdentityField = true }
+        };
+
+        if (url is not null)
+            fields.Add(new() { Name = "url", Type = FieldType.Url, Selector = url, IsRequired = true });
+
+        if (company is not null)
+            fields.Add(new() { Name = "company", Type = FieldType.String, Selector = company, IsIdentityField = true });
+
+        if (location is not null)
+            fields.Add(new() { Name = "location", Type = FieldType.String, Selector = location });
+
+        if (posted is not null)
+            fields.Add(new() { Name = "posted_date", Type = FieldType.Date, Selector = posted });
+
+        if (deadline is not null)
+            fields.Add(new() { Name = "deadline", Type = FieldType.Date, Selector = deadline, TrackHistory = false });
+
+        var identityFields = new List<string> { "title" };
+        if (company is not null) identityFields.Add("company");
+
+        return new ExtractionSchema
+        {
+            ItemSelector = itemSelector,
+            Fields = fields,
+            IdentityFieldNames = identityFields,
+            DiffSettings = new ObjectDiffSettings
+            {
+                Granularity = DiffGranularity.Both,
+                EnableImportanceScoring = true
+            }
+        };
+    }
+
+    private static FilterRule CloneRule(FilterRule source) => new()
+    {
+        Name = source.Name,
+        Description = source.Description,
+        Priority = source.Priority,
+        StopProcessing = source.StopProcessing,
+        Logic = source.Logic,
+        IsEnabled = source.IsEnabled,
+        Conditions = source.Conditions.Select(c => new FilterCondition
+        {
+            FieldName = c.FieldName, Operator = c.Operator, Value = c.Value, Negate = c.Negate
+        }).ToList(),
+        Actions = source.Actions.Select(a => new FilterAction
+        {
+            Type = a.Type, Parameters = new Dictionary<string, string>(a.Parameters)
+        }).ToList()
+    };
+}
+
+/// <summary>
+/// Definition for a single job portal watch configuration.
+/// </summary>
+public class PortalDefinition
+{
+    public required string Id { get; init; }
+    public required string Name { get; init; }
+    public required string Description { get; init; }
+    public required string Url { get; init; }
+    public required string Tier { get; init; }
+    public required TimeSpan CheckInterval { get; init; }
+    public required FetchSettings FetchSettings { get; init; }
+    public required ExtractionSchema Schema { get; init; }
+    public List<string> ExtraTags { get; init; } = [];
+}
diff --git a/src/ChangeDetection/ChangeDetection/Services/NotificationTemplateEngine.cs b/src/ChangeDetection/ChangeDetection/Services/NotificationTemplateEngine.cs
index 6ad6151..5b452c2 100644
--- a/src/ChangeDetection/ChangeDetection/Services/NotificationTemplateEngine.cs
+++ b/src/ChangeDetection/ChangeDetection/Services/NotificationTemplateEngine.cs
@@ -196,6 +196,31 @@ public partial class NotificationTemplateEngine(
             EmailBodyTextTemplate = "Page structure changed for {Watch.Name}. Selectors may need updating.",
             DiscordTitleTemplate = "ÔÜÖ´©Å {Watch.Name}",
             DiscordBodyTemplate = "Page structure changed - selectors may need updating"
+        },
+        [NotificationTemplateType.JobMatch] = new NotificationTemplate
+        {
+            Id = Guid.Parse("00000000-0000-0000-0001-000000000008"),
+            Name = "Default Job Match",
+            Type = NotificationTemplateType.JobMatch,
+            IsBuiltIn = true,
+            EmailSubjectTemplate = "­ƒö¼ Job Match: {NewValue} at {OldValue}",
+            EmailBodyHtmlTemplate = """
+                <h2>­ƒö¼ New Job Match Found</h2>
+                <p><strong>Source:</strong> {Watch.Name}</p>
+                <p><strong>URL:</strong> <a href="{Watch.Url}">{Watch.Url}</a></p>
+                <hr>
+                <h3>Match Details</h3>
+                <p>{LlmSummary}</p>
+                <hr>
+                <p><em>Detected at {Change.DetectedAt}</em></p>
+                """,
+            EmailBodyTextTemplate = """
+                Job Match Found ÔÇö {Watch.Name}
+                {LlmSummary}
+                URL: {Watch.Url}
+                """,
+            DiscordTitleTemplate = "­ƒö¼ Job Match ÔÇö {Watch.Name}",
+            DiscordBodyTemplate = "{LlmSummary}"
         }
     };
 
diff --git a/src/ChangeDetection/ChangeDetection/Services/Persistence/LiteDbContext.cs b/src/ChangeDetection/ChangeDetection/Services/Persistence/LiteDbContext.cs
index 3f08066..f43c34d 100644
--- a/src/ChangeDetection/ChangeDetection/Services/Persistence/LiteDbContext.cs
+++ b/src/ChangeDetection/ChangeDetection/Services/Persistence/LiteDbContext.cs
@@ -123,6 +123,44 @@ public class LiteDbContext : IDisposable
                 .Field(x => x.OwnerId, "OwnerId")
                 .Field(x => x.OperationType, "OperationType")
                 .Field(x => x.Status, "Status");
+
+            // FieldValueHistory
+            mapper.Entity<Core.Entities.FieldValueHistory>()
+                .Id(x => x.Id)
+                .Field(x => x.WatchedSiteId, "WatchedSiteId")
+                .Field(x => x.OwnerId, "OwnerId")
+                .Field(x => x.ObjectIdentity, "ObjectIdentity")
+                .Field(x => x.FieldName, "FieldName")
+                .Field(x => x.CapturedAt, "CapturedAt");
+
+            // WatchGroup
+            mapper.Entity<Core.Entities.WatchGroup>()
+                .Id(x => x.Id)
+                .Field(x => x.OwnerId, "OwnerId")
+                .Field(x => x.Name, "Name");
+
+            // NotificationOutboxEntry
+            mapper.Entity<Core.Entities.NotificationOutboxEntry>()
+                .Id(x => x.Id)
+                .Field(x => x.OwnerId, "OwnerId")
+                .Field(x => x.WatchedSiteId, "WatchedSiteId")
+                .Field(x => x.NotificationType, "NotificationType")
+                .Field(x => x.Destination, "Destination")
+                .Field(x => x.PayloadJson, "PayloadJson")
+                .Field(x => x.Status, "Status");
+
+            // NotificationTemplate
+            mapper.Entity<Core.Entities.NotificationTemplate>()
+                .Id(x => x.Id)
+                .Field(x => x.Name, "Name")
+                .Field(x => x.Type, "Type");
+
+            // BlockExecutionSnapshotEntity
+            mapper.Entity<Core.Entities.BlockExecutionSnapshotEntity>()
+                .Id(x => x.Id)
+                .Field(x => x.WatchId, "WatchId")
+                .Field(x => x.BlockInstanceId, "BlockInstanceId")
+                .Field(x => x.OutputJson, "OutputJson");
             
             _mapperConfigured = true;
         }
diff --git a/src/ChangeDetection/ChangeDetection/Services/ServerWatchGroupService.cs b/src/ChangeDetection/ChangeDetection/Services/ServerWatchGroupService.cs
index 86b9575..45040dc 100644
--- a/src/ChangeDetection/ChangeDetection/Services/ServerWatchGroupService.cs
+++ b/src/ChangeDetection/ChangeDetection/Services/ServerWatchGroupService.cs
@@ -27,6 +27,7 @@ public class ServerWatchGroupService(
             Description = request.Description?.Trim(),
             Icon = request.Icon,
             UserIntent = request.UserIntent,
+            AnalysisProfileJson = request.AnalysisProfileJson,
             Tags = request.Tags
         };
         await groupRepo.InsertAsync(group, ct);
diff --git a/src/ChangeDetection/ChangeDetection/Services/ServerWatchService.cs b/src/ChangeDetection/ChangeDetection/Services/ServerWatchService.cs
index b6abfff..73676f9 100644
--- a/src/ChangeDetection/ChangeDetection/Services/ServerWatchService.cs
+++ b/src/ChangeDetection/ChangeDetection/Services/ServerWatchService.cs
@@ -108,6 +108,8 @@ public class ServerWatchService : IWatchService
             TagColors = request.TagColors ?? [],
             CategoryId = request.CategoryId,
             Description = request.Description,
+            UserIntent = request.UserIntent,
+            GroupId = request.GroupId,
             LlmProviderOverride = request.LlmProviderOverride,
             FetchSettings = request.FetchSettings ?? new FetchSettings
             {
